import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keuanganmandiri.financetrackerpro',
  appName: 'Finance Tracker Pro',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
